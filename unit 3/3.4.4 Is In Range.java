/**
 * Fill this in with a Javadoc style comment.
 */
public boolean inRange(int num, int min, int max)
{
    if(num <= max && num >= min)
    {
        return true;
    }
    else
    {
        return false;
    }
}